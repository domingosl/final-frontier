{
  "name": "threejs-practice",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "rm -rf ./dist && mkdir ./dist && cp -R ./assets ./dist && cp -R ./img ./dist  && parcel ./client_src/index.html",
    "build": "rm -rf ./dist && mkdir ./dist && cp -R ./assets ./dist && cp -R ./img ./dist  && parcel build ./client_src/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.10.4",
    "three": "^0.141.0"
  },
  "staticFiles": {
    "staticPath": [
      "public"
    ]
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.0",
    "parcel": "^2.6.0",
    "parcel-plugin-static-files-copy": "^2.6.0"
  }
}
